/**
 * Must match `basePath` in next.config.mjs. GitHub Pages project sites are served at
 * https://<user>.github.io/<repo>/ so public asset URLs need this prefix.
 */
export const siteBasePath = "/q-ext-link-grid";
